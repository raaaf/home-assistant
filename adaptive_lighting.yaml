# ============================================================================
# ADAPTIVE LIGHTING CONFIGURATION
# ============================================================================
#
# Home Assistant Adaptive Lighting component configurations for all rooms
# Optimized for Bavarian climate, daily routines, and circadian rhythm health
# UPDATED: Optimized for Zigbee2MQTT with mixed Philips Hue & IKEA lights
# FIXED: Disabled detect_non_ha_changes to prevent random light turn-ons (2025-12-05)
# UPDATED 2026-02-26: Reduced Zigbee traffic & false manual_control triggers:
#   - separate_turn_on_commands: true   (nötig für IKEA fade-in)
#   - take_over_control_mode: pause_changed  (pausiert nur geänderte Attribute)
#   - adapt_only_on_bare_turn_on: false  (reverted - caused 100% brightness on motion trigger)
#
# Key principles:
# - Sleep color temps at 2200K (warmest supported by IKEA + Hue)
# - Room-specific timing based on usage patterns
# - IKEA-compatible transitions (10-15s max) to prevent bulb lockups
# - Higher brightness for safety areas (bathrooms, hallways)
# - Extended evening hours for work and outdoor spaces
# - detect_non_ha_changes: false prevents Z2M state polling from triggering lights
# - adapt_delay: 1 gives Zigbee network time to settle before adaptation

# =============================================================================
# LIVING SPACES - Primary family areas with extended evening usage
# =============================================================================

- name: "Wohnzimmer"
  lights:
    # Wohnzimmer Wand (6 lights)
    - light.ikea_light_18_wand_1
    - light.ikea_light_19_wand_2
    - light.ikea_light_20_wand_3
    - light.ikea_light_21_wand_4
    - light.ikea_light_22_wand_5
    - light.ikea_light_23_wand_6
    # Wohnzimmer Esstisch (3 lights)
    - light.philips_light_18_1
    - light.philips_light_19_2
    - light.philips_light_20_3
    # Wohnzimmer Spielematte (4 lights)
    - light.philips_light_24
    - light.philips_light_25_spielematte_2
    - light.philips_light_26_spielematte_3
    - light.philips_light_27_spielematte_4
    # Wohnzimmer Schrank (3 lights)
    - light.philips_light_21_schrank_1
    - light.philips_light_22_schrank_2
    - light.philips_light_23_schrank_3
    # Individual light
    - light.philips_light_28_spieletisch
  interval: 300  # 5 min - smooth sunrise/sunset transitions without excessive Zigbee traffic
  transition: 10  # Short transitions prevent IKEA lockups
  initial_transition: 2  # Smooth fade-in on turn_on (intercepted by AL)
  min_brightness: 20
  max_brightness: 100
  min_color_temp: 2200 # IKEA/Hue compatible warm minimum
  max_color_temp: 4000 # IKEA maximum cool white
  prefer_rgb_color: false
  sleep_brightness: 10 # Consistent across all rooms
  sleep_color_temp: 2200  # Warmest supported by all bulbs
  sleep_transition: 10 # Short transitions prevent IKEA lockups
  transition_until_sleep: true
  min_sunrise_time: "05:00:00"
  max_sunrise_time: "07:00:00" # Peak brightness/coolness at wake time
  min_sunset_time: "20:00:00"  # Dynamic: Overridden by automation based on sleep time
  max_sunset_time: "20:30:00"
  take_over_control: true
  take_over_control_mode: pause_changed  # Only pause changed attributes (not entire room)
  detect_non_ha_changes: false  # Disabled - prevents random turn-ons with Z2M state updates
  autoreset_control_seconds: 3600
  only_once: false
  adapt_only_on_bare_turn_on: false
  separate_turn_on_commands: true  # Required for IKEA fade-in (sends ON, then brightness+transition separately)
  send_split_delay: 300  # 300ms delay between ON and brightness commands (IKEA needs time to process)
  adapt_delay: 1  # Small delay lets Zigbee network settle before adaptation
  skip_redundant_commands: false  # Disabled - prevents state desync with Z2M (detect_non_ha_changes is off)
  intercept: true
  multi_light_intercept: true
  include_config_in_attributes: false
  brightness_mode: "tanh"

- name: "Küche"
  lights:
    # Küche Decke (2 lights)
    - light.ikea_light_14_decke_1
    - light.ikea_light_15_decke_2
    # Küche Insel (2 lights)
    - light.philips_light_11_oben
    - light.philips_light_12_unten
    # Küche Schrank (2 lights)
    - light.philips_light_13_schrank_1
    - light.philips_light_14_schrank_2
  interval: 300  # 5 min - smooth sunrise/sunset transitions without excessive Zigbee traffic
  transition: 10  # Short transitions prevent IKEA lockups
  initial_transition: 2  # Smooth fade-in on turn_on (intercepted by AL)
  min_brightness: 20
  max_brightness: 100
  min_color_temp: 2200 # IKEA/Hue compatible warm minimum
  max_color_temp: 4000 # IKEA maximum cool white
  prefer_rgb_color: false
  sleep_brightness: 20 # Match evening min_brightness
  sleep_color_temp: 2200  # Warmest supported by all bulbs
  sleep_transition: 10  # Short transitions prevent IKEA lockups
  transition_until_sleep: true
  min_sunrise_time: "05:00:00" # Aligned with wake time
  max_sunrise_time: "07:00:00" # Peak brightness/coolness at wake time
  min_sunset_time: "20:00:00"  # Dynamic: Overridden by automation based on sleep time
  max_sunset_time: "20:30:00"
  take_over_control: true
  take_over_control_mode: pause_changed  # Only pause changed attributes (not entire room)
  detect_non_ha_changes: false  # Disabled - prevents random turn-ons with Z2M state updates
  autoreset_control_seconds: 3600
  only_once: false
  adapt_only_on_bare_turn_on: false
  separate_turn_on_commands: true  # Required for IKEA fade-in (sends ON, then brightness+transition separately)
  send_split_delay: 300  # 300ms delay between ON and brightness commands (IKEA needs time to process)
  adapt_delay: 1  # Small delay lets Zigbee network settle before adaptation
  skip_redundant_commands: false  # Disabled - prevents state desync with Z2M (detect_non_ha_changes is off)
  intercept: true
  multi_light_intercept: true
  include_config_in_attributes: false
  brightness_mode: "tanh"

# =============================================================================
# BEDROOMS - Sleep-optimized with gentle transitions and early sunset times
# =============================================================================

- name: "Schlafzimmer"
  # Nach Umbau: Alex-Buero. Decke raumfest + Stehlampe zugewandert. min 30 (Arbeitslicht).
  lights:
    - light.ikea_light_16_1
    - light.ikea_light_17_2
    - light.ikea_light_04
  interval: 300  # 5 min - smooth sunrise/sunset transitions without excessive Zigbee traffic
  transition: 10  # Short transitions prevent IKEA lockups
  initial_transition: 2  # Smooth fade-in on turn_on (intercepted by AL)
  min_brightness: 30
  max_brightness: 100
  min_color_temp: 2200 # IKEA/Hue compatible warm minimum
  max_color_temp: 4000 # IKEA maximum cool white
  prefer_rgb_color: false
  sleep_brightness: 10 # Consistent across all rooms
  sleep_color_temp: 2200  # Warmest supported by all bulbs
  sleep_transition: 10 # Short transitions prevent IKEA lockups
  transition_until_sleep: true
  min_sunrise_time: "05:00:00"
  max_sunrise_time: "07:00:00" # Peak brightness/coolness at wake time
  min_sunset_time: "20:00:00"  # Dynamic: Overridden by automation based on sleep time
  max_sunset_time: "20:30:00"
  take_over_control: true
  take_over_control_mode: pause_changed  # Only pause changed attributes (not entire room)
  detect_non_ha_changes: false  # Disabled - prevents random turn-ons with Z2M state updates
  autoreset_control_seconds: 3600
  only_once: false
  adapt_only_on_bare_turn_on: false
  separate_turn_on_commands: true  # Required for IKEA fade-in (sends ON, then brightness+transition separately)
  send_split_delay: 300  # 300ms delay between ON and brightness commands (IKEA needs time to process)
  adapt_delay: 1  # Small delay lets Zigbee network settle before adaptation
  skip_redundant_commands: false  # Disabled - prevents state desync with Z2M (detect_non_ha_changes is off)
  intercept: true
  multi_light_intercept: true
  include_config_in_attributes: false
  brightness_mode: "tanh"

- name: "Kinderzimmer"
  lights:
    - light.ikea_light_13            # Einzellicht (Hauptlicht)
    - light.philips_light_10_bucher           # Einzellicht (Leselicht)
  interval: 300  # 5 min - smooth sunrise/sunset transitions without excessive Zigbee traffic
  transition: 10  # Short transitions prevent IKEA lockups
  initial_transition: 2  # Smooth fade-in on turn_on (intercepted by AL)
  min_brightness: 10
  max_brightness: 100
  min_color_temp: 2200 # IKEA/Hue compatible warm minimum
  max_color_temp: 4000 # IKEA maximum cool white
  prefer_rgb_color: false
  sleep_brightness: 10 # Consistent across all rooms
  sleep_color_temp: 2200  # Warmest supported by all bulbs
  sleep_transition: 10 # Short transitions prevent IKEA lockups
  transition_until_sleep: true
  min_sunrise_time: "05:00:00"
  max_sunrise_time: "07:00:00" # Peak brightness/coolness at wake time
  min_sunset_time: "20:00:00"  # Dynamic: Overridden by automation based on sleep time
  max_sunset_time: "20:30:00"
  take_over_control: true
  take_over_control_mode: pause_changed  # Only pause changed attributes (not entire room)
  detect_non_ha_changes: false  # Disabled - prevents random turn-ons with Z2M state updates
  autoreset_control_seconds: 3600
  only_once: false
  adapt_only_on_bare_turn_on: false
  separate_turn_on_commands: true  # Required for IKEA fade-in (sends ON, then brightness+transition separately)
  send_split_delay: 300  # 300ms delay between ON and brightness commands (IKEA needs time to process)
  adapt_delay: 1  # Small delay lets Zigbee network settle before adaptation
  skip_redundant_commands: false  # Disabled - prevents state desync with Z2M (detect_non_ha_changes is off)
  intercept: true
  multi_light_intercept: true
  include_config_in_attributes: false
  brightness_mode: "tanh"

# =============================================================================
# BATHROOMS & UTILITY - Safety-focused with higher night brightness
# =============================================================================

- name: "Badezimmer"
  lights:
    # Badezimmer Decke (2 lights)
    - light.philips_light_03
    - light.philips_light_04_2
    # Individual lights
    - light.philips_light_06_schrank
    - light.philips_light_05_fenster
  interval: 300  # 5 min - smooth sunrise/sunset transitions without excessive Zigbee traffic
  transition: 10  # Short transitions prevent IKEA lockups
  initial_transition: 2  # Smooth fade-in on turn_on (intercepted by AL)
  min_brightness: 20
  max_brightness: 100
  min_color_temp: 2200 # IKEA/Hue compatible warm minimum
  max_color_temp: 4000 # IKEA maximum cool white
  prefer_rgb_color: false
  sleep_brightness: 20 # Match evening min_brightness
  sleep_color_temp: 2200  # Warmest supported by all bulbs
  sleep_transition: 10  # Short transitions prevent IKEA lockups
  transition_until_sleep: true
  min_sunrise_time: "05:00:00" # Aligned with wake time
  max_sunrise_time: "07:00:00" # Peak brightness/coolness at wake time
  min_sunset_time: "20:00:00"  # Dynamic: Overridden by automation based on sleep time
  max_sunset_time: "20:30:00"
  take_over_control: true
  take_over_control_mode: pause_changed  # Only pause changed attributes (not entire room)
  detect_non_ha_changes: false  # Disabled - prevents random turn-ons with Z2M state updates
  autoreset_control_seconds: 900 # 15 min - reduced to recover quickly from Sleep-Mode false positives
  only_once: false
  adapt_only_on_bare_turn_on: false
  separate_turn_on_commands: true  # Required for IKEA fade-in (sends ON, then brightness+transition separately)
  send_split_delay: 300  # 300ms delay between ON and brightness commands (IKEA needs time to process)
  adapt_delay: 1  # Small delay lets Zigbee network settle before adaptation
  skip_redundant_commands: false  # Disabled - prevents state desync with Z2M (detect_non_ha_changes is off)
  intercept: true
  multi_light_intercept: true
  include_config_in_attributes: false
  brightness_mode: "tanh"

- name: "Waschzimmer"
  lights:
    - light.philips_light_17
  interval: 300  # 5 min - smooth sunrise/sunset transitions without excessive Zigbee traffic
  transition: 10  # Short transitions prevent IKEA lockups
  initial_transition: 2  # Smooth fade-in on turn_on (intercepted by AL)
  min_brightness: 30
  max_brightness: 100
  min_color_temp: 2200 # IKEA/Hue compatible warm minimum
  max_color_temp: 4000 # IKEA maximum cool white
  prefer_rgb_color: false
  sleep_brightness: 10 # Consistent across all rooms
  sleep_color_temp: 2200  # Warmest supported by all bulbs
  sleep_transition: 10 # Short transitions prevent IKEA lockups
  transition_until_sleep: true
  min_sunrise_time: "05:00:00"
  max_sunrise_time: "07:00:00" # Peak brightness/coolness at wake time
  min_sunset_time: "20:00:00"  # Dynamic: Overridden by automation based on sleep time
  max_sunset_time: "20:30:00"
  take_over_control: true
  take_over_control_mode: pause_changed  # Only pause changed attributes (not entire room)
  detect_non_ha_changes: false  # Disabled - prevents random turn-ons with Z2M state updates
  autoreset_control_seconds: 7200
  only_once: false
  adapt_only_on_bare_turn_on: false
  separate_turn_on_commands: true  # Required for IKEA fade-in (sends ON, then brightness+transition separately)
  send_split_delay: 300  # 300ms delay between ON and brightness commands (IKEA needs time to process)
  adapt_delay: 1  # Small delay lets Zigbee network settle before adaptation
  skip_redundant_commands: false  # Disabled - prevents state desync with Z2M (detect_non_ha_changes is off)
  intercept: true
  multi_light_intercept: true
  include_config_in_attributes: false
  brightness_mode: "tanh"

# =============================================================================
# WORK & DRESSING AREAS - Productivity-optimized with extended evening hours
# =============================================================================

- name: "Arbeitszimmer"
  # Nach Umbau: Schlafplatz + Rafael. Decke/PC raumfest + Bett/TV zugewandert. min 10 (Schlaf).
  lights:
    - light.ikea_light_03
    - light.philips_light_01_pc_1
    - light.philips_light_02_pc_2
    - light.philips_light_15_bett
    - light.philips_light_16_tv
  interval: 300  # 5 min - smooth sunrise/sunset transitions without excessive Zigbee traffic
  transition: 10  # Short transitions prevent IKEA lockups
  initial_transition: 2  # Smooth fade-in on turn_on (intercepted by AL)
  min_brightness: 10
  max_brightness: 100
  min_color_temp: 2200 # IKEA/Hue compatible warm minimum
  max_color_temp: 4000 # IKEA maximum cool white
  prefer_rgb_color: false
  sleep_brightness: 10 # Consistent across all rooms
  sleep_color_temp: 2200  # Warmest supported by all bulbs
  sleep_transition: 10 # Short transitions prevent IKEA lockups
  transition_until_sleep: true
  min_sunrise_time: "05:00:00" # Aligned with wake time
  max_sunrise_time: "07:00:00" # Peak brightness/coolness at wake time
  min_sunset_time: "20:00:00"  # Dynamic: Overridden by automation based on sleep time
  max_sunset_time: "20:30:00"
  take_over_control: true
  take_over_control_mode: pause_changed  # Only pause changed attributes (not entire room)
  detect_non_ha_changes: false  # Disabled - prevents random turn-ons with Z2M state updates
  autoreset_control_seconds: 3600
  only_once: false
  adapt_only_on_bare_turn_on: false
  separate_turn_on_commands: true  # Required for IKEA fade-in (sends ON, then brightness+transition separately)
  send_split_delay: 300  # 300ms delay between ON and brightness commands (IKEA needs time to process)
  adapt_delay: 1  # Small delay lets Zigbee network settle before adaptation
  skip_redundant_commands: false  # Disabled - prevents state desync with Z2M (detect_non_ha_changes is off)
  intercept: true
  multi_light_intercept: true
  include_config_in_attributes: false
  brightness_mode: "tanh"

- name: "Ankleide"
  lights:
    # Ankleide Decke (2 lights)
    - light.ikea_light_01
    - light.ikea_light_02
  interval: 300  # 5 min - smooth sunrise/sunset transitions without excessive Zigbee traffic
  transition: 10  # Short transitions prevent IKEA lockups
  initial_transition: 2  # Smooth fade-in on turn_on (intercepted by AL)
  min_brightness: 10
  max_brightness: 100
  min_color_temp: 2200 # IKEA/Hue compatible warm minimum
  max_color_temp: 4000 # IKEA maximum cool white
  prefer_rgb_color: false
  sleep_brightness: 10 # Consistent across all rooms
  sleep_color_temp: 2200  # Warmest supported by all bulbs
  sleep_transition: 10  # Short transitions prevent IKEA lockups
  transition_until_sleep: true
  min_sunrise_time: "05:00:00" # Aligned with wake time
  max_sunrise_time: "07:00:00" # Peak brightness/coolness at wake time
  min_sunset_time: "20:00:00"  # Dynamic: Overridden by automation based on sleep time
  max_sunset_time: "20:30:00"
  take_over_control: true
  take_over_control_mode: pause_changed  # Only pause changed attributes (not entire room)
  detect_non_ha_changes: false  # Disabled - prevents random turn-ons with Z2M state updates
  autoreset_control_seconds: 7200
  only_once: false
  adapt_only_on_bare_turn_on: false
  separate_turn_on_commands: true  # Required for IKEA fade-in (sends ON, then brightness+transition separately)
  send_split_delay: 300  # 300ms delay between ON and brightness commands (IKEA needs time to process)
  adapt_delay: 1  # Small delay lets Zigbee network settle before adaptation
  skip_redundant_commands: false  # Disabled - prevents state desync with Z2M (detect_non_ha_changes is off)
  intercept: true
  multi_light_intercept: true
  include_config_in_attributes: false
  brightness_mode: "tanh"

# =============================================================================
# TRANSITION AREAS - Motion-activated spaces with quick transitions
# =============================================================================

- name: "Flur"
  lights:
    # Flur Decke (6 lights)
    - light.ikea_light_07_decke_1
    - light.ikea_light_08_decke_2
    - light.ikea_light_09_decke_3
    - light.ikea_light_10_decke_4
    - light.ikea_light_11_decke_5
    - light.ikea_light_12_decke_6
    # Flur Schrank (2 lights)
    - light.philips_light_08_schrank_1
    - light.philips_light_09_2
    # Stahlträger
    - light.stahltraeger
  interval: 300  # 5 min - smooth sunrise/sunset transitions without excessive Zigbee traffic
  transition: 10  # Short transitions prevent IKEA lockups
  initial_transition: 2  # Smooth fade-in on turn_on (intercepted by AL)
  min_brightness: 20
  max_brightness: 100
  min_color_temp: 2200 # IKEA/Hue compatible warm minimum
  max_color_temp: 4000 # IKEA maximum cool white
  prefer_rgb_color: false
  sleep_brightness: 10 # Consistent across all rooms
  sleep_color_temp: 2200  # Warmest supported by all bulbs
  sleep_transition: 10 # Short transitions prevent IKEA lockups
  transition_until_sleep: true
  min_sunrise_time: "05:00:00"
  max_sunrise_time: "07:00:00" # Peak brightness/coolness at wake time
  min_sunset_time: "20:00:00"  # Dynamic: Overridden by automation based on sleep time
  max_sunset_time: "20:30:00"
  take_over_control: true
  take_over_control_mode: pause_changed  # Only pause changed attributes (not entire room)
  detect_non_ha_changes: false  # Disabled - prevents random turn-ons with Z2M state updates
  autoreset_control_seconds: 900 # 15 min - reduced to recover quickly from Sleep-Mode false positives
  only_once: false
  adapt_only_on_bare_turn_on: false
  separate_turn_on_commands: true  # Required for IKEA fade-in (sends ON, then brightness+transition separately)
  send_split_delay: 300  # 300ms delay between ON and brightness commands (IKEA needs time to process)
  adapt_delay: 1  # Small delay lets Zigbee network settle before adaptation
  skip_redundant_commands: false  # Disabled - prevents state desync with Z2M (detect_non_ha_changes is off)
  intercept: true
  multi_light_intercept: true
  include_config_in_attributes: false
  brightness_mode: "tanh"

# =============================================================================
# OUTDOOR AREAS - Extended evening hours with slower transitions
# =============================================================================

- name: "Balkon"
  lights:
    # Balkon Wand (2 lights)
    - light.ikea_light_05_wand_1
    - light.ikea_light_06_wand_2
    # Individual light
    - light.philips_light_07_blumen
  interval: 300  # 5 min - smooth sunrise/sunset transitions without excessive Zigbee traffic
  transition: 10  # Short transitions prevent IKEA lockups
  initial_transition: 2  # Smooth fade-in on turn_on (intercepted by AL)
  min_brightness: 30
  max_brightness: 100
  min_color_temp: 2200 # IKEA/Hue compatible warm minimum
  max_color_temp: 4000 # IKEA maximum cool white
  prefer_rgb_color: false
  sleep_brightness: 30 # Match evening min_brightness
  sleep_color_temp: 2200  # Warmest supported by all bulbs
  sleep_transition: 10 # Short transitions prevent IKEA lockups
  transition_until_sleep: false # No sleep transition for outdoor
  min_sunrise_time: "05:00:00" # Aligned with wake time
  max_sunrise_time: "07:00:00" # Peak brightness/coolness at wake time
  min_sunset_time: "20:00:00"  # Dynamic: Overridden by automation based on sleep time
  max_sunset_time: "20:30:00"
  take_over_control: true
  take_over_control_mode: pause_changed  # Only pause changed attributes (not entire room)
  detect_non_ha_changes: false  # Disabled - prevents random turn-ons with Z2M state updates
  autoreset_control_seconds: 7200 # Longer reset for outdoor
  only_once: false
  adapt_only_on_bare_turn_on: false
  separate_turn_on_commands: true  # Required for IKEA fade-in (sends ON, then brightness+transition separately)
  send_split_delay: 300  # 300ms delay between ON and brightness commands (IKEA needs time to process)
  adapt_delay: 1  # Small delay lets Zigbee network settle before adaptation
  skip_redundant_commands: false  # Disabled - prevents state desync with Z2M (detect_non_ha_changes is off)
  intercept: true
  multi_light_intercept: true
  include_config_in_attributes: false
  brightness_mode: "tanh" # Smooth transitions for outdoor
